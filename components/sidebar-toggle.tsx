import type { ComponentProps } from 'react';

// This component is deprecated and no longer used.
// The sidebar toggle functionality is now only in the sidebar itself.
export function SidebarToggle({ className }: ComponentProps<any>) {
  return null;
}
